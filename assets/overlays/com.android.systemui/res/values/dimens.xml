<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Height of the status bar header bar -->
    <dimen name="status_bar_header_height">72dp</dimen>

    <!-- Elevation for the header in the pulldown -->
    <dimen name="status_bar_expanded_header_elevation">0dp</dimen>

    <!-- Side padding for the expanded status bar header -->
    <dimen name="notification_header_side_padding">0dp</dimen>

    <!-- Side padding for the QS panel -->
    <dimen name="qs_panel_side_padding">0dp</dimen>

    <!-- Bottom padding for QS panel -->
    <dimen name="qs_panel_padding_bottom">0dp</dimen>

    <!-- Distance that QS panel sticks out below header -->
    <dimen name="qs_peek_height">0dp</dimen>

    <!-- The padding between the individual notification cards. -->
    <dimen name="notification_padding">0dp</dimen>

    <!-- The padding between the individual notification cards when dimmed. -->
    <dimen name="notification_padding_dimmed">0dp</dimen>

    <!-- radius of the corners of the material rounded rect background -->
    <dimen name="notification_material_rounded_rect_radius">0dp</dimen>

    <!-- Side padding for notifications -->
    <dimen name="notification_side_padding">0dp</dimen>

    <!-- Top padding for notifications -->
    <dimen name="notifications_top_padding">0dp</dimen>

    <!-- Distance between notifications -->
    <dimen name="z_distance_between_notifications">0.01dp</dimen>

    <!-- What does this do -->
    <dimen name="notification_children_divider_height">0.5dip</dimen>

    <!-- Height of a small notification in the status bar -->
    <dimen name="notification_min_height">72dp</dimen>

    <!-- The height of the speed bump view. -->
    <dimen name="speed_bump_height">0dp</dimen>

</resources>